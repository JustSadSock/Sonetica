{
  "name": "sonetica",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "predev": "npm run fetch:fonts && node scripts/gen-icons.mjs",
    "build": "tsc -b && vite build",
    "prebuild": "npm run fetch:fonts && node scripts/gen-icons.mjs",
    "preview": "vite preview",
    "lint": "tsc --noEmit",
    "fetch:fonts": "node scripts/fetch-fonts.mjs",
    "gen:icons": "node scripts/gen-icons.mjs",
    "test": "vitest run"
  },
  "devDependencies": {
    "@tsconfig/node18": "^18.2.4",
    "@types/node": "^24.3.0",
    "jsdom": "^24.0.0",
    "sharp": "^0.33.4",
    "typescript": "^5.9.2",
    "vite": "^7.1.3",
    "vitest": "^2.1.1"
  },
  "engines": {
    "node": ">=18"
  }
}
